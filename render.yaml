services:
  - type: web
    name: mel-bot-website
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"  # Replace with the actual command to start your app
    branch: main
